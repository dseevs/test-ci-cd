/**
 * BACKWARD COMPATIBILITY — use  import { getTranslation } from "@/i18n"
 */
export {
  LAB_LANGUAGE_STORAGE_KEY,
  getCurrentLanguage,
  setCurrentLanguage,
  getTranslation,
  getTranslation as getLabText,
} from "./index"
